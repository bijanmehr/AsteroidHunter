"""Pure-stdlib simulation core (uses only math, random, array, json).

Nothing in this package may import numpy, gymnasium, or any third-party
library — that is what lets the same code run natively for training and in the
browser under Pyodide.
"""
